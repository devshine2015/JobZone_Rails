<%= form_with(model: message, url:(message.persisted? ? job_convesation_message_url(message) : job_convesation_messages_url), local: true) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :message_body %>
    <%= form.text_area :message_body, id: :message_message_body %>
  </div>

  <div class="field">
    <%= form.label :message_type %>
    <%= form.number_field :message_type, id: :message_message_type %>
  </div>

  <div class="field">
    <%= form.label :conversation_id %>
    <%= form.text_field :conversation_id, id: :message_conversation_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :message_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
