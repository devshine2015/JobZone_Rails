<p id="notice"><%= notice %></p>

<h1>Jobs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Location</th>
      <th>Status</th>
      <th>Description</th>
      <th>User</th>
      <th>Company</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @candidate_jobs.each do |candidate_job| %>
      <%- job = candidate_job.job %>
      <tr>
        <td><%= job.title %></td>
        <td><%= job.city %></td>
        <td><%= job.status_id %></td>
        <td><%= job.description %></td>
        <td><%= job.user %></td>
        <td><%= job.company %></td>
        <td><%= link_to 'Show', job %></td>
        <td><%= link_to 'Edit', edit_job_path(job) %></td>
        <td><%= link_to 'Destroy', job, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Job', new_job_path %>
<%= link_to 'Convesations', user_conversations_path %>
